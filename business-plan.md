# Token Factory 商业计划书

> **一句话定位**:在"用户/Agent"与"模型厂商"之间,架设一层智能上下文精炼与路由层,用"更准确的放弃策略"替用户把通用 Agent 的无谓 token 消耗打下来,并从中赚取价值差。

---

## 一、执行摘要

智能路由与上下文精炼,解决的是通用 Agent(LMS 类,如 Workbody、TRAE Workflow、CodeX)最隐蔽的成本黑洞:**"无差别全量塞入上下文"**。

- 一个"你好"级别的简单任务,在通用 Agent 上可能消耗 **15 万 token**——因为系统提示、Skill 定义、用户规则、历史对话、OCR 全文被整体塞入上下文,其中 99% 对回答当前问题毫无用处。
- 我们通过"**上下文路由 + 前置精炼 + 工具层替代**",把同一任务的真实 token 消耗压回到千级——**数量级的差距,而非几个百分点**。
- 商业模式上我们规划两条路径:**按量计价**(透明、易起步)与**按价值计价**(黑盒、利润空间大),两者可先后递进。

**核心主张**:智能路由的终极降本,不是让大模型跑得更快,而是**让大模型尽量别跑**。省下的重复计算,就是我们的利润空间。

---

## 二、问题定义

### 2.1 通用 Agent 的 token 黑洞

通用 Agent 为了"省事 + 高成功率",默认把几乎所有内容塞进上下文,让模型自己"淘金":

| 塞入内容 | 量级 | 对简单任务的有用性 |
|---------|------|------------------|
| 系统提示词 | 千~万 | 低 |
| Harness / Skill 定义 | 千~万 | 低 |
| 用户自定义规则 | 千~万 | 低 |
| 历史对话上下文 | 万 | 低 |
| 上传文件 OCR 全文 | 万~十万 | 很低 |
| 工具 / 函数 schema | 千 | 低 |

传统问答 Workflow 只塞"当前问题 + 相关片段"(千级);通用 Agent 则全量塞入(万~十万级)。**这是结构性差异,不是偶然浪费。**

### 2.2 两个核心矛盾

1. **"判断问题该去哪"的元问题**——如果路由判断本身太费算力,就本末倒置。
2. **"动态 vs 静态"的 token 悖论**——写死规则会因模型/算力变化而过时;写成动态则永远多花 0.3~1 倍 token 做判断。

**破局思路**:把"该筛的内容"从"全塞给模型"中独立出来,用**静态规则 + 轻量判断 + 动态账本**分层承接,让"额外判断"只花在未覆盖的长尾上。

---

## 三、解决方案概述

我们在用户/Agent 与模型厂商之间架设一层 **Token Factory 精炼层**:

```
传统:  用户请求 ── 全量塞入 ──▶ 模型厂商 ──▶ 全量算钱
我们:  用户请求 ──▶ [Token Factory]
              │  ① 拆包分类(系统提示/Skill/OCR/历史/用户输入)
              │  ② 前置精炼(便宜模型按规则压缩)
              │  ③ 智能路由(判断层 + 工具层)
              └──▶ 只传"真正需要的" ──▶ 模型厂商 ──▶ 按实际算钱
```

**价值主张**:你(模型厂商)按 token 收费,而 Token Factory 能让同一个任务少消耗 90% 的 token。省下的钱就是我们的利润空间。

---

## 四、技术架构

### 4.1 核心原则:判断与执行解耦

**路由器 ≠ 执行器。** 路由器不必是执行层任何一个模型,而是独立的"判断器":

- **路由器**:用便宜的专用判断模型(7B 级),只做"分类/分流",不做"回答"
- **执行器**:4 个真实模型按擅长领域分工

### 4.2 四层 Skill 架构

```
┌────────────────────────────────────────────┐
│  入口层:接收上游请求,拆包分类                │
│  (系统提示/Skill定义/OCR/历史/用户输入)      │
├────────────────────────────────────────────┤
│  判断层:最便宜模型,判定"该走哪条路"           │
│  → 工具能解决?(数学/查表/格式化) → 走工具     │
│  → 需精炼后交大模型?      → 走精炼+大模型     │
├────────────────────────────────────────────┤
│  工具层:各 Skill(计算器/压缩/查DB/文档转换)   │
│  → 每个 Skill 负责一个确定性"动作"           │
├────────────────────────────────────────────┤
│  模型接口层:统一封装 Deepseek/字节/Kimi 等接口 │
│  → 统一契约,透传/收尾                        │
└────────────────────────────────────────────┘
```

### 4.3 智能路由:四层漏斗(降本主线)

| 层级 | 机制 | 成本 | 拦截比例 |
|------|------|------|---------|
| 第 1 层 | 硬规则与启发式(正则/长度/关键词) | ≈0 | 30%~40% |
| 第 2 层 | 语义哈希 + 语义缓存(向量检索) | ~1/1000 | 累积 60% |
| 第 3 层 | 轻量分类器(静态,CPU 可跑) | 极低 | 累积累 |
| 第 4 层 | 动态账本 + 级联兜底(长尾) | 低 | 兜底 |

### 4.4 前置精炼(最见效的降本利器)

用**最便宜的 7B 模型**先读一遍、按规则压缩,再把"精华"传给贵模型:

```
用户原想:  全量内容 → 传给贵模型(贵)
我们的方案: 全量内容 → 便宜7B按规则压缩 → 精华传给贵模型
```

**经济学本质:token 与 token 有区别——用便宜的 token 替换昂贵的 token。** 最坏情况仅是多花一次便宜 7B 模型的调用,成本可忽略。

**压缩规则分层(防丢关键信息):**

| 优先级 | 内容 | 动作 |
|-------|------|------|
| P1 必须保留 | 数字/日期/金额/代码/条款序号 | 原样保留 |
| P2 尽量保留 | 结论句/任务目标/用户原始指令 | 尽量保留 |
| P3 可压缩 | 重复表达/过渡句/无关 OCR | 删除/缩减 |
| 锚点 | 被压缩内容记录(段落号/关键词) | 便于回溯 |

### 4.5 外置存储与工具化

- **Skill 内嵌 Python 脚本**做批处理,把"计算"交给代码而非 token。
- **轻量 DB(SQLite)** 存大数据,AI 只需一条查询命令定位结果,不塞全量。
- **索引化 Skill 库**:按需加载,规避"规则爆炸"——规则多不多不重要,关键是**每次只加载命中的极小部分**。

### 4.6 缓存的事实/表达分离

针对"评价拿破仑"这类**事实固定但表达可变**的任务:

- **缓存"事实包"**(固定):命中省去重复检索,省 token。
- **保留"现场表达"**(可变):每次仍由 AI 现场组织观点与措辞,保留"AI 的味道"。

**省的是"重复检索",不是"现场思考"。**

---

## 五、数据资产与自我进化

### 5.1 现有数据

我们已持有来自 Workbody、CodeX 的**约几十亿 token**(对应上百万条调用记录、几十 GB 文本)的真实调用数据。

### 5.2 数据驱动设计

**方法论:数据定当下,经验+想象定未来。**

| 数据能告诉 | 数据不能告诉 |
|-----------|-------------|
| 流量分布、token 分布、哪些高频/肥 | 未来需求、模型如何变化、用户如何用 |
| 哪些任务高频、哪些板块最肥 | 架构的未来扩展方向 |

**分析路径**:抽样看结构 → 全量验证 → 聚焦"既高频又费 token"的靶心(二八定律)。

### 5.3 反馈闭环(不依赖用户点赞)

- **显式反馈(赞踩)在真实场景几乎收集不到**,不依赖它。
- **隐式信号**才是主力:用户是否复制答案、是否追问更深入、是否换问法重问、会话是否持续推进。
- **会话结算(反刍)**:会话结束后统一定论,reward 回填到会话用过的模型上。
- **动态账本**:维护"(任务簇, 模型) → 胜率/延迟/成本/样本数",用 Bandit 思想(利用 + 少量探索)让系统跟随模型能力变化自适应。

---

## 六、商业模式

### 6.1 两条定价路径

**路线 A:按量计价(透明,先落地)**

```
卖价: 官方价格的 80%(打八折)
成本: 官方 100% - 省掉的 token
利润: 省下的 - 额外消耗的便宜 token
```

**核心等式:平均省 token 比例 > 20% 即有得赚。** 而精炼/工具/缓存的潜力是几十倍,20% 远低于能力上限。

**必须算清的两个隐藏成本**:
1. 判断层/精炼层自身的 token 消耗(用便宜的 7B 模型压到最低)。
2. 省不动的复杂请求(创意/推理)会净亏,需靠"省得多的"补贴。

**进阶建议**:按请求类型定价(省得多的给大折扣、省不动的收原价),而非一刀切八折,避免被"永久八折"承诺套死。

**路线 B:按价值计价(黑盒,利润放大器)**

```
内部用什么模型,用户不需要知道
售价: 市场认可的"理想价"
  → 比最便宜模型贵,但比最贵的模型便宜很多
  → 同时能解决"最贵模型才能解决"的问题
利润源: 价值差(与 token 成本解耦)
```

**优势**:定价由"市场价值"决定,不由成本锁死,利润空间大。
**短板**:需要建立"你能解决贵模型问题"的信任(Benchmark、案例、效果付费)。

### 6.2 两条路线可递进

```
先做路线 A(按量、透明、易起步) → 积累技术 + 数据 + 案例
再升级路线 B(按价值、赚大利润) → 用实力与信任放大利润
```

### 6.3 资本打法(可选)

在"做得不够好、省不动的请求较多"的阶段,可引入资本补贴抢用户:

```
旗号: 比官方永久打八折
本质: 用资本杠杆买"抢占市场的时间窗口"
赌注: 在窗口期内把优化做深,做到"补贴停掉后也能赚钱"
```

**清醒认知**:资本补贴不是赚钱,是买时间;必须在窗口期内实现"自洽盈利"。

---

## 七、竞争分析

| 维度 | 传统 API 转售 | 通用路由中间件 | 我们的 Token Factory |
|------|--------------|---------------|---------------------|
| 聚焦点 | 卖 token | 选模型 | 选模型 + 精炼上下文 + 工具替代 |
| 竞争壁垒 | 无(纯差价) | 低(易被复制) | 数据飞轮 + 定点吃透头部 Agent |
| 优化深度 | 无 | 浅 | 数量级(上下文路由) |

**护城河**:不是"精炼"这个动作,而是"精炼决策"这个**越用越准的数据飞轮**——需要海量真实流量 + 对各家模型脾气的了解,别人难以短期复制。

---

## 八、落地路线图

**阶段 1:数据洞察(MVP 前提)**
- 清洗、结构化手头几十亿 token 数据。
- 抽样 → 全量验证 → 找出"既高频又费 token"的靶心。
- 产出"平均能省多少"的关键数字,校验商业账。

**阶段 2:定点优化(吃透 1 家)**
- 针对一个头部 Agent(TRAE Workflow 或 Workbody)做前置精炼 + 工具层。
- 实测"省了多少 token + 答得准不准",形成真实案例。

**阶段 3:架构化(抽象共性)**
- 定型四层 Skill 架构,把精炼规则技能化、索引化。
- 外置 DB、批处理脚本、缓存(事实/表达分离)逐步接入。

**阶段 4:商业化(两条路径递进)**
- 先走路线 A(按量八折),用真实案例拉新。
- 积累数据与信誉后,升级路线 B(按价值定价)。

**技术优先级**:
1. 前置精炼层(最省、最见效)
2. 索引化 Skill 库(按需加载,规避全量塞入)
3. 外置 DB(大数据挪出上下文)

---

## 九、风险与对策

| 风险 | 影响 | 对策 |
|------|------|------|
| 压缩丢关键信息 | 回答质量下降 | 压缩规则分级(P1/P2/P3)+ 原文锚点回溯 |
| 上游流量结构变化 | 省不动、账算不过来 | 数据驱动 + 按请求类型定价,不承诺一刀切永久折扣 |
| 精炼层自身成本 | 侵蚀利润 | 用最便宜 7B 模型 + 静态规则,压到近乎 0 |
| "永久八折"套死 | 定价困死 | 定价留余地 / 按省多少计费 |
| 竞争同质化 | 无壁垒 | 养数据飞轮 + 定点吃透头部 Agent |

---

## 十、团队与执行信念

- **技术路线明确**:智能路由 + 前置精炼 + 工具层 + 数据飞轮。
- **数据在手**:约几十亿 token 真实调用数据,支持数据驱动设计。
- **已验证经验**:团队具备自研开源 Skill 的落地经验(索引化、按需加载)。
- **执行信念**:先定点吃透一家,再抽象为通用;用真实省 token 案例说话,商业自然成立。

---

## 十一、结语

> **Token Factory 的定位**:不是卖 token 的中间商,而是**重组 token 的生产过程**来创造价值。
>
> **核心理念**:智能路由/缓存的终极降本,不是让大模型跑得更快,而是**让大模型尽量别跑**。缓存是第一道闸,小模型是第二道闸,大模型是最后才开的闸。
>
> **两条商业路径**:按量计价(透明、先落地)与按价值计价(黑盒、赚大钱),先走 A 再升 B。
>
> **一句话**:我们用一个几乎免费的"放弃策略",替用户把通用的算力浪费降到数量级之下——省下的重复计算,就是我们的价值。